Readme
# 🍎 Apple Leaf Disease Detection

1. Clone the repo and place `best_model.pth` in the root folder  
2. Install dependencies: `pip install -r requirements.txt`  
3. Run the app: `streamlit run app.py`  
4. Upload your `.pth` model and a test image in the browser  
5. View predicted disease class and confidence chart
